apiVersion: v1
kind: Service

metadata:
  name: internship-service

  labels:
    app: internship-app

spec:
  type: NodePort

  selector:
    app: internship-app

  ports:
    - 
      name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30009